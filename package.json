{
  "name": "hummus-workshop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check dist/app.js && node --check dist/calculations.js && node --check dist/storage.js && node --test tests/*.test.mjs",
    "build": "node scripts/check.mjs"
  }
}
